# Docker-optimized dependencies
# Focuses on pre-built wheels to avoid compilation issues
# Avoids problematic packages: Rust-based, numpy conflicts

# Core RAG (pre-built wheels, tested versions)
langchain==0.3.13
langchain-openai==0.2.14
langchain-community==0.3.13
langchain-core==0.3.29
langchain-text-splitters==0.3.5

# OpenAI API
openai==1.58.1

# Vector Store - FAISS pre-built wheel
faiss-cpu==1.9.0.post1

# Token counting (pure Python)
tiktoken==0.8.0

# Document Processing - stable versions with wheels
PyPDF2==3.0.1
python-docx==0.8.11
openpyxl==3.1.4
pandas==2.2.3

# Web Framework
streamlit==1.39.0
streamlit-option-menu==0.4.0

# Core utilities (wheel-based)
python-dotenv==1.0.1
pydantic==2.10.5

# Logging & Progress
tqdm==4.67.1
rich==13.9.4

# HTTP client
requests==2.32.3

# Additional utilities (pure Python / wheel-based)
# typing-extensions==4.10.0
